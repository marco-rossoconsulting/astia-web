---
import BaseLayout from '@layouts/BaseLayout.astro';
import PricingContent from '@components/page-content/PricingContent.astro';
import { getEntry } from 'astro:content';

const lang = 'en' as 'en' | 'de' | 'it';
const page = await getEntry('pages', 'pricing');
const seo = page.data.seo;
const d = page.data as any;

const siteUrl = 'https://astiaweb.com';
const priceValidUntil = `${new Date().getFullYear() + 1}-12-31`;

const offerSchemas = [
  {
    '@context': 'https://schema.org',
    '@type': 'Offer',
    name: 'Self-Serve — Up to 15 rooms',
    description: d.matrix.selfTagline[lang],
    url: `${siteUrl}/pricing`,
    price: d.matrix.prices.selfSmall,
    priceCurrency: d.matrix.currency,
    billingIncrement: 1,
    priceValidUntil,
    priceSpecification: {
      '@type': 'PriceSpecification',
      price: d.matrix.prices.selfSmall,
      priceCurrency: d.matrix.currency,
      unitCode: 'MON',
      description: 'Monthly subscription',
    },
    itemOffered: {
      '@type': 'Service',
      name: 'Self-Serve',
      areaServed: 'Hotel and Accommodation',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Offer',
    name: 'Self-Serve — Up to 30 rooms',
    description: d.matrix.selfTagline[lang],
    url: `${siteUrl}/pricing`,
    price: d.matrix.prices.selfMedium,
    priceCurrency: d.matrix.currency,
    billingIncrement: 1,
    priceValidUntil,
    priceSpecification: {
      '@type': 'PriceSpecification',
      price: d.matrix.prices.selfMedium,
      priceCurrency: d.matrix.currency,
      unitCode: 'MON',
      description: 'Monthly subscription',
    },
    itemOffered: {
      '@type': 'Service',
      name: 'Self-Serve',
      areaServed: 'Hotel and Accommodation',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Offer',
    name: 'Self-Serve — 50+ rooms',
    description: d.matrix.selfTagline[lang],
    url: `${siteUrl}/pricing`,
    price: d.matrix.prices.selfLarge,
    priceCurrency: d.matrix.currency,
    billingIncrement: 1,
    priceValidUntil,
    priceSpecification: {
      '@type': 'PriceSpecification',
      price: d.matrix.prices.selfLarge,
      priceCurrency: d.matrix.currency,
      unitCode: 'MON',
      description: 'Monthly subscription',
    },
    itemOffered: {
      '@type': 'Service',
      name: 'Self-Serve',
      areaServed: 'Hotel and Accommodation',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Offer',
    name: 'Managed — Up to 15 rooms',
    description: d.matrix.managedTagline[lang],
    url: `${siteUrl}/pricing`,
    price: d.matrix.prices.managedSmall,
    priceCurrency: d.matrix.currency,
    billingIncrement: 1,
    priceValidUntil,
    priceSpecification: {
      '@type': 'PriceSpecification',
      price: d.matrix.prices.managedSmall,
      priceCurrency: d.matrix.currency,
      unitCode: 'MON',
      description: 'Monthly subscription',
    },
    itemOffered: {
      '@type': 'Service',
      name: 'Managed',
      areaServed: 'Hotel and Accommodation',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Offer',
    name: 'Managed — Up to 30 rooms',
    description: d.matrix.managedTagline[lang],
    url: `${siteUrl}/pricing`,
    price: d.matrix.prices.managedMedium,
    priceCurrency: d.matrix.currency,
    billingIncrement: 1,
    priceValidUntil,
    priceSpecification: {
      '@type': 'PriceSpecification',
      price: d.matrix.prices.managedMedium,
      priceCurrency: d.matrix.currency,
      unitCode: 'MON',
      description: 'Monthly subscription',
    },
    itemOffered: {
      '@type': 'Service',
      name: 'Managed',
      areaServed: 'Hotel and Accommodation',
    },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'Offer',
    name: 'Managed — 50+ rooms',
    description: d.matrix.managedTagline[lang],
    url: `${siteUrl}/pricing`,
    price: d.matrix.prices.managedLarge,
    priceCurrency: d.matrix.currency,
    billingIncrement: 1,
    priceValidUntil,
    priceSpecification: {
      '@type': 'PriceSpecification',
      price: d.matrix.prices.managedLarge,
      priceCurrency: d.matrix.currency,
      unitCode: 'MON',
      description: 'Monthly subscription',
    },
    itemOffered: {
      '@type': 'Service',
      name: 'Managed',
      areaServed: 'Hotel and Accommodation',
    },
  },
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: d.faq.items.map((item: any) => ({
    '@type': 'Question',
    name: item.q[lang],
    acceptedAnswer: {
      '@type': 'Answer',
      text: item.a[lang],
    },
  })),
};
---
<BaseLayout lang={lang} page="pricing" seo={seo} currentPath="/pricing" schemas={[...offerSchemas, faqSchema]}>
  <PricingContent lang={lang} />
</BaseLayout>
